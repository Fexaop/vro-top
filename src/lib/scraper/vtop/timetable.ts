import type { VtopSession } from '@/types/auth';
import type { TimetableEntry } from '@/types/attendance';

export async function fetchTimetableData(_session: VtopSession): Promise<TimetableEntry[]> {
  // TODO Phase 3
  throw new Error('fetchTimetableData not yet implemented');
}
